[credential]
    username = antonstrand

[user]
    name = Anton Strand
    email = jag@antonstrand.se

[includeIf "gitdir/i:~/work/"]
    [user]
        name = Anton Strand
        email = anton.strand@insurello.se

[core]
    editor = nvim
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true
    line-numbers = true
    side-by-side = true
    line-numbers-left-format = ""
    line-numbers-right-format = "│ "

[log]
    date = iso

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default
